name: GitHub Actions My Notions
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  frontend-pipeline:
    env:
      working-directory: ./my-notions-static
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [ 17.x ]
    steps:
      - uses: actions/checkout@v2
      - name: Starting Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: install modules
        run: npm install
      - name: build production project
        run: npm build:prod
      - name: linting typescript
        run: npm lint:ts
      - name: linting scss
        run: npm lint:scss
      - name: run unit tests
        run: npm test:unit
